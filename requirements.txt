yt-dlp
validators
ffmpeg-python
dotenv
inference
moviepy
supervision
roboflow



# Non-Python dependencies required:
#
# Node.js (https://nodejs.org) — required for YouTube bot-detection bypass
#   macOS:          brew install node  →  https://brew.sh if you don't have Homebrew
#   Windows:        https://nodejs.org/download.html
#   Linux (apt):    sudo apt install nodejs npm
#   Linux (yum):    sudo yum install nodejs npm
#   Linux (Arch):   sudo pacman -S nodejs npm
#
# ffmpeg (https://ffmpeg.org) — required for merging video/audio streams
#   macOS:          brew install ffmpeg
#   Windows:        https://ffmpeg.org/download.html
#   Linux (apt):    sudo apt install ffmpeg
#   Linux (yum):    sudo yum install ffmpeg
#   Linux (Arch):   sudo pacman -S ffmpeg
